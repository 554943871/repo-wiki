# Expected Wiki Doctor Report

Mode: audit-only.

Gate note: `wiki/07-drift.md` is already in the standard empty state.

Classification: `safe_guidance_rewrite`.

Proposed actions: `rewrite_page`, `add_table_or_diagram`.

No stable wiki pages were rewritten.

Proposed reader-refresh shape:

- Split the dense Pricing paragraph into responsibility, caller, manual override, uncertainty, and evidence notes.
- Keep the promo rounding uncertainty visible instead of turning it into a stable fact.
