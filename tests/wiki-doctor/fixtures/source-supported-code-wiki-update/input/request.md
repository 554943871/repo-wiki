# Request Snippet

The user says current code may disagree with the wiki: checkout totals might now come from Pricing rather than Cart Store.

This is intentionally a source-supported update request. `wiki-doctor` should inspect direct source evidence and rewrite the stable page when the evidence is clear enough to cite.
