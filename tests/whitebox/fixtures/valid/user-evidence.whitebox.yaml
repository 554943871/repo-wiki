kind: whitebox_component_diagram
version: 1
component:
  id: checkout_module
  label: Checkout Module
  evidence:
  - source: user
    note: User confirmed the module boundary during issue 17 fixture setup.
  ports:
  - id: submit_order
    label: Submit Order
externals:
- id: external_actor
  label: External Actor
connectors:
- id: actor_submits_order
  type: external
  label: submits order
  evidence:
  - source: user
    note: User confirmed the actor-to-boundary interaction.
  from: external_actor
  to: checkout_module.submit_order
